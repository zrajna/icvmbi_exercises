function I = basic_registration(moved, ref)
%BASIC_REGISTRATION translation between two images
%   basic_registration(moved, ref) returns translated version of moved,
%   registered to ref

    % your code comes here, start with removing the following line
    I = moved;
    
end
